{"id": "codex-abc15d424e", "state": {"cli": "codex", "version": "0.155.1", "entry": "New local TUI sessions now leave reasoning summaries disabled by default, fixing request rejection by providers that do not support them. Explicit reasoning-summary settings remain respected. (#46467)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "guard", "touches_agm": true}, "meta": {"cli": "codex", "relevant": true}}
{"id": "codex-7361458176", "state": {"cli": "codex", "version": "0.155.0", "entry": "Added experimental `/voice` conversations with live transcripts and microphone controls on supported builds, enabled through `/experimental`. (#43581, #43651, #44331)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-9f7b1914a9", "state": {"cli": "codex", "version": "0.155.0", "entry": "The TUI now shows live reasoning summaries in the status row and completion timestamps after successful turns. (#43558, #43921)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "guard", "touches_agm": true}, "meta": {"cli": "codex", "relevant": true}}
{"id": "codex-5ba052f348", "state": {"cli": "codex", "version": "0.155.0", "entry": "Added task hiding, archiving, and deletion in the agents overview, plus worktree ownership details and confirmed deletion of clean managed worktrees. (#43942, #44424, #44433)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-c3343d18bf", "state": {"cli": "codex", "version": "0.155.0", "entry": "Added Touch ID verification for MCP requests in local TUI sessions on supported Macs. (#43624, #43712, #43715)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-ea082a70d9", "state": {"cli": "codex", "version": "0.155.0", "entry": "Added configurable daemon update schedules and `codex app-server daemon update`; saved threads and active goals can recover after daemon restarts. (#43542, #43562, #44314)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "adopt", "touches_agm": true}, "meta": {"cli": "codex", "relevant": true}}
{"id": "codex-f1b2d3080e", "state": {"cli": "codex", "version": "0.155.0", "entry": "Amazon Bedrock can now obtain AWS credentials from configured commands, with caching, expiration-based refresh, and authentication recovery. (#44028)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-14e39c6281", "state": {"cli": "codex", "version": "0.155.0", "entry": "Accepted prompts are now saved even when compaction fails before a turn starts. (#44487)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-1a6132cf70", "state": {"cli": "codex", "version": "0.155.0", "entry": "Fixed missed tmux resizes, transcript viewport restoration, and stale history appearing after switching threads. (#43603, #43889, #43994)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-cd9195b5b9", "state": {"cli": "codex", "version": "0.155.0", "entry": "MCP servers now report expired OAuth credentials accurately and provide reconnect guidance when token refresh fails. (#43947, #44359)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-1ceba75e1c", "state": {"cli": "codex", "version": "0.155.0", "entry": "Automatic approval reviews now preserve complete actions and authorization evidence more reliably, retry transient failures, and distinguish review failures from unsafe-action findings. (#44482, #44569, #44570)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-a98220da05", "state": {"cli": "codex", "version": "0.155.0", "entry": "Switching accounts now invalidates remote-control sessions, cached WebSocket state, and model catalogs belonging to the previous identity. (#43906, #44341, #44489)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "guard", "touches_agm": true}, "meta": {"cli": "codex", "relevant": true}}
{"id": "codex-878afc456e", "state": {"cli": "codex", "version": "0.155.0", "entry": "Blocked Windows-process escapes from restricted WSL sandboxes and hardened brokered shell snapshots against credential exposure. (#44286, #43909, #44040)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "codex-9c8062c825", "state": {"cli": "codex", "version": "0.155.0", "entry": "Aligned Python SDK and runtime publishing with stable CLI releases, using matching versions and verifying runtime assets before SDK publication. (#44067)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "codex", "relevant": false}}
{"id": "claude-01", "state": {"cli": "claude", "version": "2.1.277", "entry": "Added AGENTS.md support: in a project with no CLAUDE.md, Claude Code reads AGENTS.md instead; change it under \"Project instructions\" in `/config` (not yet on Bedrock, Vertex or Foundry)", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "guard", "touches_agm": true}, "meta": {"cli": "claude", "relevant": true}}
{"id": "claude-02", "state": {"cli": "claude", "version": "2.1.277", "entry": "Added an optional `headers:` map on Claude apps gateway upstreams, to send static headers to a proxy you run in front of a provider", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-03", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed `claude -p` and Agent SDK sessions that could hang with no result after an internal error; they now report the error and exit with code 1", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-04", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed being unexpectedly logged out when an older Claude Code build (for example an IDE extension's bundled CLI) runs on the same machine as the current one", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-05", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed update checks erroring every 30 minutes, and `claude update` hanging when a minimum or maximum version is set, if a proxy returns an invalid version; a malformed `minimumVersion` is now ignored", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-06", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed `claude plugin install` sometimes failing and breaking the installed copy when reinstalling a plugin version that a session or another program was using; an unchanged copy is now left alone", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-07", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed the Write tool silently ending the turn as a declined permission when the target path is an existing directory; it now reports a clear error", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-08", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed the Edit tool reporting \"Invalid regular expression: regular expression too large\" instead of \"String not found in file\" when a very large edit containing non-ASCII text did not match the file", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-09", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed background sessions (`claude --bg`) exiting when a plugin's LSP server exited or closed its stdin", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-10", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed a crash at launch when `~/.claude.json` holds a malformed `theme` value", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-11", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed a crash when resuming a session whose saved history holds an assistant message stored as a plain string", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-12", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed a rare case where the screen could stop updating for the rest of the session after an internal rendering error", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-13", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed sessions continued after `/clear` (restart, `--continue`, `--resume`) missing part of their first message when a SessionStart hook printed output, causing a full prompt-cache miss", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "upgrade-arg", "touches_agm": true}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-14", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed the \"copied\" notice not appearing after drag-selecting text in the fullscreen `/resume` picker and other panels that cover the prompt area", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-15", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed WebFetch and WebSearch in Cowork cloud sessions not telling Claude why a request was refused, such as a used-up fetch budget or an admin policy", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-16", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed one malformed `strictKnownMarketplaces` or `blockedMarketplaces` entry silently disabling the whole enterprise marketplace policy", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-17", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed `/plugin` not stripping terminal control characters from messages on the Installed tab, such as the error of a failed plugin update", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-18", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed `/plugin` closing with no message when every install in a multi-select failed", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-19", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed plugins from the official marketplace being recorded without their commit in `installed_plugins.json`, and `installed_plugins.json` keeping the old commit after updating a pinned-commit plugin", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-20", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed Remote Control session bookkeeping failing when `~/.claude.json` holds a malformed placeholder record", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-21", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed typed or pasted text occasionally coming out scrambled in the `claude agents` dispatch input during key repeat or very fast input", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-22", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed Enter on a selected agent panel row doing nothing when `keybindings.json` rebinds Enter in the Chat context, for example to `chat:queueSubmit`", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-23", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed a headless resume (`claude -p --resume`, the SDK, a VS Code extension window reload) starting the session's cost and usage totals at zero; headless sessions now save their totals at exit", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-24", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed a `sandbox.excludedCommands` glob exempting an entire compound Bash command from the sandbox when only one part matched; every part must now match", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-25", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed rate-limited artifact publishes telling Claude to stop retrying; Claude is now told nothing was published and when to send the same publish again", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-26", "state": {"cli": "claude", "version": "2.1.277", "entry": "Fixed Console sign-in showing only \"Request failed with status code 400\" when the server refuses to create an API key; it now shows the server's message", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-27", "state": {"cli": "claude", "version": "2.1.277", "entry": "Improved session start-up for SDK and headless (`-p`) use: the first turn no longer waits on the per-directory CLAUDE.md lookup", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-28", "state": {"cli": "claude", "version": "2.1.277", "entry": "Improved `/plugin` Installed: an MCP server listed apart from its plugin now shows which plugin it belongs to", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-29", "state": {"cli": "claude", "version": "2.1.277", "entry": "Improved the startup notice overflow line under the logo: it now reads \"N more notices hidden\" instead of \"+N more · /status\"", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "guard", "touches_agm": true}, "meta": {"cli": "claude", "relevant": true}}
{"id": "claude-30", "state": {"cli": "claude", "version": "2.1.277", "entry": "Improved `/ultrareview` when there's nothing to review: messages say which case you're in, offer a command that reviews your latest commit, and a new repository's first commit is reviewed in full", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-31", "state": {"cli": "claude", "version": "2.1.277", "entry": "Improved the dangerous-rm permission prompt to name the flagged rm command and suggest a `${VAR:?}` guard, so headless runs can recover", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-32", "state": {"cli": "claude", "version": "2.1.277", "entry": "Changed Fable to always appear in `/model` on the Anthropic API; it is greyed out only when your organization's settings disable it", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "guard", "touches_agm": true}, "meta": {"cli": "claude", "relevant": true}}
{"id": "claude-33", "state": {"cli": "claude", "version": "2.1.277", "entry": "Changed `/ultrareview` in non-interactive sessions to refuse when the repository has no base branch or shared history", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-34", "state": {"cli": "claude", "version": "2.1.277", "entry": "Changed workflow scripts' computed `agent()` prompts on Bedrock, Vertex and Foundry to reach the subagent framed as script-authored text, so the safety classifier does not read them as the user", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-35", "state": {"cli": "claude", "version": "2.1.277", "entry": "Removed the deprecated TaskOutput tool; Claude reads a background task's output file with Read instead, and the `taskOutputMaxChars` setting and `TASK_MAX_OUTPUT_LENGTH` no longer have any effect", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
{"id": "claude-36", "state": {"cli": "claude", "version": "2.1.277", "entry": "[VSCode] Added background shells and other running tasks to the agent map, each with a Stop, and a typed `/tasks` that opens it", "agm_depends_on": ["SessionStart / Stop / StopFailure / Notification hooks", "text drawn on the terminal screen (status row, banners, dialogs, menus, composer)", "--resume and saved session transcripts", "statusline JSON", "usage-limit / rate-limit notices and /usage output", "login state and switching between accounts", "/model and /effort pickers driven by key presses", "project instruction files (CLAUDE.md)"]}, "questions": {"touches_agm": {"type": "noul", "instructions": "Does `entry` change the behaviour of anything listed in `agm_depends_on`?"}, "verdict": {"type": "choice", "instructions": "`entry` is one changelog line of a coding-agent CLI. A supervisor program drives that CLI unattended through the things in `agm_depends_on`. How should the supervisor's maintainers react?", "criteria": {"guard": {"what": "What the supervisor reads or relies on now looks or behaves differently, so its integration may silently misread or break and needs checking", "not_for": "bug fixes that only make the CLI more correct without changing what the supervisor sees"}, "adopt": {"what": "A new capability the supervisor could start using to do its job better"}, "upgrade-arg": {"what": "A fix for a failure the supervisor could actually hit; nothing to change, just a reason to upgrade"}, "none": {"what": "Unrelated to the supervisor: IDE extensions, plugins, cloud providers, Windows, cosmetics, features it does not use"}}}}, "labels": {"verdict": "none", "touches_agm": false}, "meta": {"cli": "claude", "relevant": false}}
